[project]
name = "deepscientist"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "deepagents",
    "langchain>=1.1.3",
    "langchain-anthropic>=1.3.0",
    "langchain-openai>=1.1.3",
    "python-dotenv>=1.2.1",
]

[tool.uv.sources]
deepagents = { git = "https://github.com/j0yk1ll/deepagents.git", subdirectory = "libs/deepagents" }
