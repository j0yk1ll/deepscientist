# DeepScientist Environment Configuration

# =============================================================================
# LLM Configuration
# =============================================================================
# Model identifier (e.g., gpt-4.1-mini, gpt-4.1, claude-3-opus, etc.)
LM_MODEL=gpt-4.1-mini
# Optional: Base URL for OpenAI-compatible endpoints (LM Studio, OpenRouter, etc.)
LM_BASE_URL=
# API key for the LLM provider
LM_API_KEY=
# Temperature for model responses (0.0 = deterministic)
LM_TEMPERATURE=0.0
# Maximum input tokens before summarization triggers
LM_MAX_INPUT_TOKENS=32768

# =============================================================================
# Literature Retrieval Engine Configuration
# =============================================================================
# GROBID service URL (HTTP endpoint)
RETRIEVAL_GROBID_URL=http://localhost:8070

# Contact email for Unpaywall API requests
RETRIEVAL_UNPAYWALL_EMAIL=you@example.com

# Default timeout (seconds) for outbound HTTP requests
RETRIEVAL_REQUEST_TIMEOUT_S=30.0

# =============================================================================
# DeepScientist Workspace
# =============================================================================
# Root directory for agent workspace (files, memories, scratchpad)
WORKSPACE=./workspace